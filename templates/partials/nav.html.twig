<nav class="d-flex border justify-content-between align-items-end gap-4 p-4">
    <h2>
        <a href="{{ path('home') }}">Quizz</a>
    </h2>

    <ul class="d-flex list-unstyled gap-4">
        {% if user %}
        <li>
            <a href="{{ path('profile') }}">Profile</a>
        </li>
        <li>
            <a href="{{ path('logout') }}">Logout</a>
        </li>
        {% endif %}

        {% if user == null %}
        <li>
            <a href="{{ path('register') }}">Register</a>
        </li>
        <li>
            <a href="{{ path('login') }}">Login</a>
        </li>
        {% endif %}

        {% if is_granted('ROLE_ADMIN') %}
        <li>
            <a href="{{ path('dashboard') }}">Dashboard</a>
        </li>
        {% endif %}
    </ul>
</nav>